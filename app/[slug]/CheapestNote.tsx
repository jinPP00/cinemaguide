import type { Branch } from '@/lib/types';

/**
 * 요금표의 최저/최고를 자동 평가하는 문장은 제거한다.
 * 가격은 표 자체로 제공하고 사용자가 직접 비교할 수 있게 한다.
 */
export default function CheapestNote({ branch: _branch }: { branch: Branch }) {
  return null;
}
